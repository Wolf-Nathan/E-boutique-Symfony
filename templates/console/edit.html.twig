{% extends 'base.html.twig' %}

{% block title %}Modifier {{ console.nom }}{% endblock %}

{% block body %}
    <h1>Modifier {{ console.nom }}</h1>

    <div class="container">
    {{ include('console/_form.html.twig', {'button_label': 'Modifier la console'}) }}

    <a href="{{ path('console_index') }}">Retourner à la liste des consoles</a>

    {{ include('console/_delete_form.html.twig') }}
    </div>

{% endblock %}
