<form method="post" action="{{ path('adresse_delete', {'id': adresse.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette adresse?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adresse.id) }}">
    <button class="btn">Supprimer l'adresse</button>
</form>